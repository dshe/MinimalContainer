distribution options:

1) Nuget package which installs a portable assembly targeting netstandard1.0.
	UWP apps error:
	Package restore failed. Rolling back package changes for 'App'.
	StandardContainer is not compatible with UAP,Version=v10.0.
	nuget pack places dll files inside portable50 folder rather than the netstandard1.0 folder
	https://github.com/NuGet/Home/issues/3108
	http://dist.nuget.org/index.html
	NuGet bugs.

2) NuGet package which installs the one source file.
	Could not install package:
	You are trying to install this package into a project that targets '.NETFramework,Version=v4.6',
	but the package does not contain any assembly references or content files that are compatible with that framework.
	http://blog.nuget.org/20141001/targetframeworkfiltering.html
	http://blog.nuget.org/20160126/nuget-contentFiles-demystified.html
    NuGet bugs.

3) Manual copy of source file to project.
	inconvenient
	no auto updates
	no namespace modification
	no download monitoring
	no NuGet

===================================
try ndepend

 IFoo
 IBar

 Ioc, Dependency-Injection, WinRT, Windows-Runtime, Inversion-of-Control

 GitHub
 AppVeyor
 Nuget

 AppVeyor

 Cake:
 Invoke-WebRequest http://cakebuild.net/download/bootstrapper/windows -OutFile build.ps1


 identify artifacts (results of the build) 

 project Nuget feed:
 Project feed contains all NuGet packages pushed to artifacts during the build process. Project feed is read-only. Publish your custom packages to the account feed. 
 https://ci.appveyor.com/nuget/standardcontainer-0jkhcpau93q3
 
 account Nuget feed:
 Account feed aggregates NuGet packages from all projects. You can publish your custom NuGet packages to the account feed and then use it as an additional NuGet source during the build process.
 https://ci.appveyor.com/nuget/dshe-ofhaerbtr093
 
 With cloud apps like AppVeyor for build/test/deploy, services like like RayGun.io for error management, Trello for products backlogs, FreshDesk for support tickets, 


 StandardContainer.Source
 n ultra lightweight single file Inversion of Control container for the .Net framework.
 ******************************************************************************
    LightInject version 4.1.2 (NET46)
    http://www.lightinject.net/
    http://twitter.com/bernhardrichter

 
appveyor.yaml

version: 1.0.{build}
image: Visual Studio 2015
build_script:
- cmd: PowerShell -Version 2.0 .\build.ps1
test: off